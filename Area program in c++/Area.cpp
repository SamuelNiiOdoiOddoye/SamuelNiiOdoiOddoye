#include <iostream>
using namespace std;
int main(void) {
    // this program accepts the first input to be the lenght, the second to be the width and diplay the area with the formula 
    //Area=Length*width
    cout<< "Type in the length below" << endl;
    int length;
    cin >> length<< endl;
    Cout<< "Type in the width below" << endl;
    int width;
    cin >> width << endl;
    int Area;
    Area=Length*width;
    Cout << Area;
    
    return 0;
}
